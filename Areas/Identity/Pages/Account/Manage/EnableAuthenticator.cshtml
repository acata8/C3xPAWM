@page
@model EnableAuthenticatorModel
@{
    ViewData["Title"] = "Configura app di autenticazione";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h4>@ViewData["Title"]</h4>
<div>
    <p>Per usare un app di autenticazione esegui i seguenti step:</p>
    <ol class="list">
        <li>
            <p>
                Installa un app di 2FA come Microsoft Authenticator per
                <a href="https://go.microsoft.com/fwlink/?Linkid=825072">Android</a> e
                <a href="https://go.microsoft.com/fwlink/?Linkid=825073">iOS</a> oppure
                Google Authenticator per
                <a href="https://play.google.com/store/apps/details?id=com.google.android.apps.authenticator2&amp;hl=en">Android</a> e
                <a href="https://itunes.apple.com/us/app/google-authenticator/id388497605?mt=8">iOS</a>.
            </p>
        </li>
        <li>
            <p>Scannerizza il QR Code or inserisci la chiave <kbd>@Model.SharedKey</kbd> nella tua app di autenticazione. Spazi e maiuscole non contano.</p>
            <div class="alert alert-info">Impara a <a href="https://go.microsoft.com/fwlink/?Linkid=852423">abilitare la generazione di QR code</a>.</div>
            <div id="qrCode"></div>
            <div id="qrCodeData" data-url="@Html.Raw(@Model.AuthenticatorUri)"></div>
        </li>
        <li>
            <p>
                Dopo aver scansionato il codice QR o inserito la chiave sopra, l'app di autenticazione a due fattori ti fornirà
                  con un codice univoco. Inserisci il codice nella casella di conferma sottostante.
            </p>
            <div class="row">
                <div class="col-md-6">
                    <form id="send-code" method="post">
                        <div class="form-group">
                            <label asp-for="Input.Code" class="control-label"> Codice di verifica</label>
                            <input asp-for="Input.Code" class="form-control" autocomplete="off" />
                            <span asp-validation-for="Input.Code" class="text-danger"></span>
                        </div>
                        <button type="submit" class="btn btn-primary">Verifica</button>
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    </form>
                </div>
            </div>
        </li>
    </ol>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
