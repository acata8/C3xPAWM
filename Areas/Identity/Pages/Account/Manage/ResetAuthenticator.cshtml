@page
@model ResetAuthenticatorModel
@{
    ViewData["Title"] = "Reimposta chiave di autenticazione";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h4 class="title">@ViewData["Title"]</h4>
<div class="alert alert-warning" role="alert">
    <p class="font-weight-bold">
        Se reimposti la chiave di autenticazione, l'app di autenticazione non funzionerà finché non la riconfiguri  
    </p>
    <p>
        Questo processo disabilita l'autenticazione a due fattori fino a quando non verifichi la tua app di autenticazione. <br>
         Se non completi la configurazione dell'app di autenticazione potresti perdere l'accesso al tuo account.
    </p>
</div>
<div>
    <form id="reset-authenticator-form" method="post" class="form-group">
        <button id="reset-authenticator-button" class="btn btn-danger" type="submit">Reimposta chiave di autenticazione</button>
    </form>
</div>