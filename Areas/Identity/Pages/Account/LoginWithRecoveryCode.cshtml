@page
@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "Verifica del codice di ripristino";
}

<p class="color h3">@ViewData["Title"]</p>
<hr />
<p class="color mt-2 h5">
    Hai richiesto di accedere con un codice di ripristino. <br> Questo accesso non verrà ricordato fino a quando non fornirai
     un codice app di autenticazione all'accesso o disattiva 2FA e accedi di nuovo.
</p>
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                
                <input asp-for="Input.RecoveryCode" class="form-control" autocomplete="off" placeholder="Codice di ripristino" />
                <span asp-validation-for="Input.RecoveryCode" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-outline-primary w-100 "> <i class="fas fa-key"></i>Login</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
