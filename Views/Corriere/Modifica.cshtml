 @model CorriereInputModel

@{
    ViewData["Title"] = "Modifica Negozio";
}

<p class="color mt-3 h3">Modifica i tuoi dati</p>


@using((Html.BeginForm("Modifica", "Corriere", FormMethod.Post))){
        
    @Html.HiddenFor(m => m.CorriereId)
    
    <div class="row mt-3">
        <div class="col-md-12">
            <div asp-validation-summary="All"></div>
        </div>
    </div>
    <div class="row mt-2">
        <div class="col-md-4 mt-3">
            <label for="Nominativo">Nome Azienda</label>
            <input class="form-control" asp-for="Nominativo" type="text">
        </div>
    </div>

    <div class="row mt-3">
         <div class="col-md-4">
            <label for="Telefono">Telefono</label>
            <input class="form-control" asp-for="Telefono" type="text">
        </div>
    </div>

    <div class="row mt-3">

        <div class="col-md-4">
            <button type="submit" class="btn btn-outline-primary w-100 "><i class="fas fa-save"></i>&nbsp; Salva</button>
        </div>
    </div>
}